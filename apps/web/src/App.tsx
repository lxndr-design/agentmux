export function App() {
  return (
    <main className="shell">
      <h1>agentmux</h1>
      <p>
        One shell, many agents, one conscience — the IDE shell lands in later PRs. This stub proves
        the build and render pipeline.
      </p>
    </main>
  );
}
